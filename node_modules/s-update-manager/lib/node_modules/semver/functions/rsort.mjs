import { c as compareBuild_1 } from './compare-build.mjs';

const compareBuild = compareBuild_1;
const rsort = (list, loose) => list.sort((a, b) => compareBuild(b, a, loose));
var rsort_1 = rsort;

export { rsort_1 as r };
