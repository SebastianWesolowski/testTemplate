import { ConfigTemplateType } from '@/feature/config/types';
export declare const prepareTemplateFile: ({ config, fileList, }: {
    config: ConfigTemplateType;
    fileList: string[] | [];
}) => Promise<{
    config: ConfigTemplateType;
    fileList: string[] | [];
    templateFileList: string[] | [];
}>;
