import { ConfigTemplateType } from '@/feature/config/types';
export declare const scanProjectFolder: (config: ConfigTemplateType) => Promise<{
    config: ConfigTemplateType;
    fileList: string[] | [];
}>;
