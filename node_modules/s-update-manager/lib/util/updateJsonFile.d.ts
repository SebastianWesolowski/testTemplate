import { FileMapConfig } from '@/feature/updateFileMapConfig';
export declare const updateJsonFile: ({ filePath, config, newContent, replaceFile, }: {
    filePath: string;
    config: {
        isDebug: boolean;
    } & Record<string, any>;
    newContent: object;
    replaceFile?: boolean | undefined;
}) => Promise<FileMapConfig | null>;
