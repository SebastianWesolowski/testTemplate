/// <reference types="node" />
interface CreateFileParams {
    filePath?: string;
    folderPath?: string;
    fileName?: string;
    content: string | Buffer;
    options?: {
        createFolder?: boolean;
        overwriteFile?: boolean;
        backupFile?: boolean;
    };
    isDebug?: boolean;
}
export declare function createFile({ filePath, fileName, folderPath, content, options, isDebug, }: CreateFileParams): Promise<string>;
export {};
