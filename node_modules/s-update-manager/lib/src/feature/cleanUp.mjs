import { deletePath } from '../util/deletePath.mjs';

const cleanUp = async (config) => {
    try {
        await deletePath(config.temporaryFolder, true);
        return config;
    }
    catch (error) {
        console.error(`Error isnt exist: ${error.message}`);
        return config;
    }
};

export { cleanUp };
