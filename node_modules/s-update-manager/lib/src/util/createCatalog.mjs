import fs from 'fs';

async function createCatalog(filePath) {
    try {
        await fs.promises.mkdir(filePath, { recursive: true });
        return filePath;
    }
    catch (error) {
        console.error('Błąd podczas tworzenia katalogu:', error);
        throw error;
    }
}

export { createCatalog };
